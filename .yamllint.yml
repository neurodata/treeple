extends: default

ignore: |
  treeple/_lib/
  .asv/

rules:
  line-length: disable
  document-start: disable
  truthy: disable
  braces:
    forbid: false
    min-spaces-inside: 0
    max-spaces-inside: 1
